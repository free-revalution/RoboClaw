// src/vision/frame_processor.cpp
#include "frame_processor.h"

namespace roboclaw::vision {

// FrameProcessor implementations are pure virtual or have default inline implementations
// This file provides non-inline implementations for derived classes if needed in the future
// and ensures the header is properly included in the build system.

// LambdaProcessor::process() is defined inline in the header

} // namespace roboclaw::vision
